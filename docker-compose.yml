services:
  db:
    image: mysql:8.0
    container_name: boggle-mysql-db
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: boggle_db
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: